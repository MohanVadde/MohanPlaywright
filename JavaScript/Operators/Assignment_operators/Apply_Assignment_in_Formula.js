// Problem: Use *= and -= in formula
// Description: Create a variable `price = 100`. First increase price by 10% using *= 1.1, then apply a discount of ₹15 using -=.
// Print the final amount.

let price = 100
price *= 1.1
price -= 15
console.log(price)